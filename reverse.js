const greeting = 'Hello, how are you?';

function reverseString(text) {
    let reverse = "";
    for (const letter of text) {
        //jokon amra rever korte cai{
        console.log(letter)
        reverse = reverse + letter;

    }

}
const reversed = reverseString(greeting);




